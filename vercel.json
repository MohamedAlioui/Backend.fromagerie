{
  "version": 2,
  "name": "fromagerie-fullstack",
  "buildCommand": "npm run build:full",
  "installCommand": "npm install && cd ../frontend && npm install",
  "routes": [
    {
      "src": "(.*)",
      "dest": "/server.js"
    },
    {
      "src": "/(.*)",
      "dest": "/server.js"
    }
  ]
}
